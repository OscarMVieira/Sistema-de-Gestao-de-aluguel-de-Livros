criar basedados_bd e inserir este codigo no SQL

CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(100),
    email VARCHAR(100) UNIQUE,
    password VARCHAR(255),
    documento VARCHAR(20),
    tipoContaId INT DEFAULT 3,
    foto VARCHAR(255) DEFAULT 'default_perfil.png'

);

--  cria o Admin aqui!! 
INSERT INTO users (username, email, password, documento, tipoContaId) 
VALUES ('Admin Fixo', 'admin@teste.com', '123', '000', 1);